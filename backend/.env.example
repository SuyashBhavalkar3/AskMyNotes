# Environment variables for the voice backend service
# copy this file to `.env` and fill in the values before running

# OpenAI API key used for speech-to-text and text-to-speech
OPENAI_API_KEY=

# URL of the Python API server (used to forward the transcribed question for
# RAG-based answers). Defaults to http://localhost:8000 if unset.
PYTHON_API_BASE=http://localhost:8000

# Optionally override the port the voice service listens on
PORT=5001
